%% prisma_flow.tex — Fluxograma PRISMA-ScR
%% Incluído via \input{prisma_flow} no figure environment de main.tex
%% Contagens exatas após busca expandida e triagem refinada de 2026.

\usetikzlibrary{positioning, calc, arrows.meta}

\begin{tikzpicture}[
  %% caixas do fluxo principal
  mbox/.style={
    rectangle, draw=black, thick, rounded corners=2pt,
    minimum width=6.8cm, text width=6.6cm,
    minimum height=1.3cm, align=center,
    fill=white, font=\small
  },
  %% caixas de exclusão (margem direita)
  ebox/.style={
    rectangle, draw=black, thick, rounded corners=2pt,
    minimum width=5.0cm, text width=4.8cm,
    minimum height=1.3cm, align=flush left,
    fill=gray!8, font=\small
  },
  %% setas
  arr/.style={-{Stealth[length=5pt,width=4pt]}, thick},
  %% labels de fase
  phase/.style={font=\bfseries\scriptsize, rotate=90, anchor=center}
]

%% ================================================================
%% FASE 1 — IDENTIFICAÇÃO
%% ================================================================
\node[mbox, minimum width=6.0cm, text width=5.8cm] (dbbox) at (-1.8, 0) {
  \textbf{Bases de dados eletrônicas}\\[2pt]
  $n = 5.621$\\[2pt]
  {\scriptsize Web of Science: 1.719\\
    Scopus: 1.580\\
    PubMed/MEDLINE: 1.059\\
    CINAHL/EBSCO: 586\\
    Cochrane Library: 399\\
    Google Scholar: 278}
};

\node[mbox, minimum width=5.0cm, text width=4.8cm,
      right=1.5cm of dbbox] (otherbox) {
  \textbf{Outras fontes (Snowballing)}\\[2pt]
  $n = 7$\\[2pt]
  {\scriptsize (Rastreamento manual de\\
    referências bibliográficas)}
};

%% ================================================================
%% FASE 2 — TRIAGEM
%% ================================================================
\coordinate (midid) at ($(dbbox.south)!0.5!(otherbox.south)$);

\node[mbox, below=2.6cm of midid, anchor=north] (dedup) {
  \textbf{Registros das bases após deduplicação}\\[2pt]
  $n = 3.189$\quad{\scriptsize (5.621 $-$ 2.432 duplicatas)}
};

%% seta das bases para deduplicação
\draw[arr] (dbbox.south) -- ++(0,-0.8cm) -| (dedup.north);

\node[mbox, below=1.2cm of dedup] (screen1) {
  \textbf{Triagem por título}\\[2pt]
  $n = 3.189$
};

\draw[arr] (dedup) -- (screen1);

\node[ebox, right=2.2cm of screen1] (excl1) {
  \textbf{Excluídos por título:} $n = 2.680$\\[3pt]
  {\scriptsize
    EC1 (Sem NLP de Diálogo): 1.120\\
    EC2 (Foco geral/EHR): 584\\
    EC3 (Diagnóstico de voz): 134\\
    EC4 (Tipo de publicação): 281\\
    EC5 (Desenho metodológico): 358\\
    EC6 (Cenários simulados): 203
  }
};

\draw[arr] (screen1.east) -- (excl1.west);

\node[mbox, below=1.2cm of screen1] (screen2) {
  \textbf{Triagem por resumo}\\[2pt]
  $n = 509$
};

\draw[arr] (screen1) -- (screen2);

\node[ebox, right=2.2cm of screen2] (excl2) {
  \textbf{Excluídos por resumo:} $n = 474$\\[3pt]
  {\scriptsize
    EC1 (Sem NLP de Diálogo): 183\\
    EC2 (Foco geral/EHR): 116\\
    EC3 (Diagnóstico de voz): 29\\
    EC4 (Tipo de publicação): 43\\
    EC5 (Desenho metodológico): 61\\
    EC6 (Cenários simulados): 42
  }
};

\draw[arr] (screen2.east) -- (excl2.west);

%% ================================================================
%% FASE 3 — ELEGIBILIDADE
%% ================================================================
\node[mbox, below=1.2cm of screen2] (eligib) {
  \textbf{Texto completo avaliado}\\[2pt]
  $n = 35$ (bases) $+$ $7$ (snowballing)
};

\draw[arr] (screen2) -- (eligib);

%% snowballing entra diretamente na elegibilidade (bypass triagem)
\draw[arr] (otherbox.south) -- ++(0,-0.8cm) -| ([xshift=1.1cm]eligib.east) -- (eligib.east);

\node[ebox, right=2.2cm of eligib] (excl3) {
  \textbf{Excluídos na íntegra:} $n = 18$ (bases); $0$ (snowballing)\\[3pt]
  {\scriptsize
    EC1 (Foco tecnológico): 4\\
    EC2 (Foco comunicacional): 3\\
    EC3 (Tipo de estudo/dados): 3\\
    EC4 (Tipo de publicação): 2\\
    EC5 (Desenho metodológico): 4\\
    EC6 (Desenho do cenário): 2
  }
};

\draw[arr] (eligib.east) -- (excl3.west);

%% ================================================================
%% FASE 4 — INCLUÍDOS
%% ================================================================
\node[mbox, below=1.2cm of eligib] (included) {
  \textbf{Estudos incluídos na síntese}\\[2pt]
  $n = 24$\\[2pt]
  {\scriptsize (17 bases + 7 snowballing)}
};

\draw[arr] (eligib) -- (included);

%% ================================================================
%% LABELS DE FASE (margem esquerda)
%% ================================================================
\node[phase] at ($(dbbox.west) + (-1.1cm, 0)$)      {IDENTIFICAÇÃO};
\node[phase] at ($(dedup.west)  + (-1.1cm, 0)$)      {TRIAGEM};
\node[phase] at ($(screen1.west) + (-1.1cm, 0)$)      {};   %% continua triagem
\node[phase] at ($(screen2.west) + (-1.1cm, 0)$)      {};   %% continua triagem
\node[phase] at ($(eligib.west) + (-1.1cm, 0)$)      {ELEGIBILIDADE};
\node[phase] at ($(included.west) + (-1.1cm, 0)$)    {INCLUÍDOS};

%% ================================================================
%% NOTA DE RODAPÉ
%% ================================================================
\node[font=\scriptsize, anchor=north west, text width=12cm]
  at ($(included.south west) + (-1.5cm, -0.5cm)$) {%
  Contagens baseadas na busca expandida de 2026.
  $\kappa = 0{,}78$ (F.E.S.\ e M.M.; kappa de Cohen).
};

\end{tikzpicture}